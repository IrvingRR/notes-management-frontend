"use client";

import { Loader } from '@/common'

/**
 * This loading component will be render the Loader component when the fetch fails.
 */

const Loading = () => {
  return (
    <Loader/>
  )
}

export default Loading;