"use client";

import styled from 'styled-components';

export const NotesContainer = styled.div`
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-wrap: wrap;
    gap: 20px;
    padding: 20px 0;
    max-height: 500px;
    overflow: auto;
`;